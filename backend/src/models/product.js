const db = require("../config/database.js");

class Product {
    static getById(id) {
        // Implemente a lógica para obter um produto pelo ID do banco de dados
    }

    static updatePrice(id, newPrice) {
        // Implemente a lógica para atualizar o preço de um produto no banco de dados
    }
}
module.exports = Product;
